
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Book {
  book_id    Int     @id @default(autoincrement())
  name String
  price  String?
}